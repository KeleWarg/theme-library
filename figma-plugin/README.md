# Figma Plugin - Component Syncer

Extracts component metadata from Figma and syncs to the admin dashboard.

## Setup

```bash
npm install
npm run build
```

Import `manifest.json` into Figma via **Plugins → Development → Import plugin from manifest**.

## Usage

1. Select a Component or Component Set
2. Run the plugin
3. Click **Sync to Dashboard**
